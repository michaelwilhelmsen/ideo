# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.*
jscpd-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# #36's contact sheets: the harness is committed, its inputs and outputs are
# not. The sources are the user's own generated material, read from the local
# store rather than copied into the repo — the rule `spikes/post-effects` states
# for its own three — and the sheets are regenerated by running it.
spikes/contact-sheet/sources/
spikes/contact-sheet/out/
